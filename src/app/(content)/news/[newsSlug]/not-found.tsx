export default function NewsNotFoundPage() {
    return (
      <div id="erro">
        <h1>Not found!</h1>
        <p>Unfortunately, we could not find the requested article.</p>
      </div>
    );
  }